const config = {
  salt: '',
  secret: '',
  pageCount: 20,
  // mysql config
  port: 3001,
  mysql: {
    host: 'localhost',
    port: 3306,
    user: 'bitzo',
    password: '123456',
    database: 'temp',
  },
};

module.exports = config;
